---
description: 需求 · Requirement — 任务描述 → 开发文档待审核（<task description>）
---

先加载并遵守技能 dev-review 的 SKILL.md：优先项目级 `.cursor/skills/dev-review/SKILL.md`（ZCode 为 `.zcode/skills/`），没有则读用户级 `~/.cursor/skills/dev-review/SKILL.md`（ZCode 为 `~/.zcode/skills/`）。

执行 dev-review 的子命令「需求」/ Run dev-review subcommand `需求`，参数：$ARGUMENTS
（参数：任务描述 / task description。写完状态=待审核，请作者去面板点「调整」或「通过」，不要开工 / status 待审核, do not start coding。）

按技能执行，不要往仓库写模板文件。开发文档固定写在 `docs/开发文档/<模块>/`。
