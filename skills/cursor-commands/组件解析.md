---
description: 扫描 widgets 建组件卡注册表，发现重复组件（组件解析 [模块]）
---

扫描组件目录建**组件注册表** `docs/组件文档/`：每个可复用组件一张卡（路径/双维标签/职责/参数 API/最小示例/使用方），标签从 `_标签.md` 受控词表取（能力=干什么、场景=用在哪，Obsidian 原生 `#能力/xx` 可全局搜索）；`_索引.md` 按能力标签分组；一次性的私有小 widget 不入册。**发现不同模块功能雷同的组件时不直接重构**，生成「组件抽取」需求文档（待审核，走审核-开工流程）。

先加载并遵守技能 dev-review 的 SKILL.md：优先项目级 `.cursor/skills/dev-review/SKILL.md`（ZCode 为 `.zcode/skills/`），没有则读用户级 `~/.cursor/skills/dev-review/SKILL.md`（ZCode 为 `~/.zcode/skills/`）。

执行 dev-review 的子命令「组件」，动作 `解析`，参数：$ARGUMENTS

按技能执行，不要往仓库写模板文件。开发文档固定写在 `docs/开发文档/<模块>/`。
