---
description: 重新获取对方接口并产出字段级变动清单（接口变动 <服务>）
---

按 `_接口.md` 记录的来源重新获取对方接口资料，与现有文档逐接口比对，产出**字段级变动清单**（新增 / 删除 / 变更）；更新接口文档、原始快照与索引；问询作者后可在 `docs/开发文档/` 生成适配需求（待审核，走审核-开工流程），不直接改业务代码。

先加载并遵守技能 dev-review 的 SKILL.md：优先项目级 `.cursor/skills/dev-review/SKILL.md`（ZCode 为 `.zcode/skills/`），没有则读用户级 `~/.cursor/skills/dev-review/SKILL.md`（ZCode 为 `~/.zcode/skills/`）。

执行 dev-review 的子命令「接口」，动作 `变动`，参数：$ARGUMENTS

按技能执行，不要往仓库写模板文件。开发文档固定写在 `docs/开发文档/<模块>/`。
