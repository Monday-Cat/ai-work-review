---
description: 按需求描述推荐复用哪个已有组件（组件查 <需求描述>）
---

**选取推荐，标签优先**：从需求描述提取能力/场景关键词 → 按 `#能力/…` `#场景/…` 标签命中缩小候选 → 读职责与示例定夺 → 推荐＋最小用法；没有合适的直说，问是否新建（交付时 `组件登记` 入库）。写 UI 前先跑这个，别重复造轮子；也可在 Obsidian 全局搜标签人工定位。

先加载并遵守技能 dev-review 的 SKILL.md：优先项目级 `.cursor/skills/dev-review/SKILL.md`（ZCode 为 `.zcode/skills/`），没有则读用户级 `~/.cursor/skills/dev-review/SKILL.md`（ZCode 为 `~/.zcode/skills/`）。

执行 dev-review 的子命令「组件」，动作 `查`，参数：$ARGUMENTS

按技能执行，不要往仓库写模板文件。开发文档固定写在 `docs/开发文档/<模块>/`。
